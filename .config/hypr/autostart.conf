# Autostart

# Pass Wayland session env to systemd so Elephant/Walker can launch GUI apps correctly
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP XDG_SESSION_TYPE XDG_RUNTIME_DIR DISPLAY
exec-once = systemctl --user daemon-reload
exec-once = systemctl --user restart elephant

# Walker + Elephant (same as walker-launch) so the first menu open is not cold
exec-once = ~/.local/bin/walker-launch --prewarm

exec-once = mako
exec-once = hyprpaper
exec-once = hypridle
exec-once = wl-paste --watch cliphist store
exec-once = [workspace 8 silent] obsidian
exec-once = [workspace 10 silent] ghostty -e btop
exec-once = [workspace 10 silent] firefox
exec-once = [workspace 2] brave --disable-features=WaylandWpColorManagerV1
exec-once = bash -c 'sleep 10 && waybar'
exec-once = bash -c 'sleep 20 && insync start --qt-qpa-platform=xcb'
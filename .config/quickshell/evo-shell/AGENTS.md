# Evo shell

Development notes for agents live in the **evoshell** Cursor skill.

**Invoke:** `/evoshell` in chat, then describe the task (e.g. `/evoshell add a bar widget for …`).

**Skill file:** `~/.cursor/skills/evoshell/SKILL.md`

Config root: `~/.config/quickshell/evo-shell/`

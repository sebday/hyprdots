#!/bin/bash
# Set yazi theme from current (generated by process_templates)

YAZI_THEME="$HOME/.themes/current/yazi-theme.toml"
YAZI_CONFIG_DIR="$HOME/.config/yazi"

[ -f "$YAZI_THEME" ] || exit 0

mkdir -p "$YAZI_CONFIG_DIR"
cp "$YAZI_THEME" "$YAZI_CONFIG_DIR/theme.toml"
echo "Set yazi theme"
